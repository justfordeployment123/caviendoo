name: Deploy Frontend

on:
  push:
    branches: [main]
    paths:
      - 'caviendoo-frontend/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install
        working-directory: caviendoo-frontend
        run: npm install

#      - name: Lint
#        working-directory: caviendoo-frontend
#        run: npm run lint

      - name: Build
        working-directory: caviendoo-frontend
        run: npm run build
        env:
          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}

      - name: Success
        run: echo "Frontend Build successful"
